<%= form_with(model: proyecto, local: true) do |form| %>
  <% if proyecto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(proyecto.errors.count, "error") %> prohibited this proyecto from being saved:</h2>

      <ul>
      <% proyecto.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre, id: :proyecto_nombre %>
  </div>

  <div class="field">
    <%= form.label :urlgithu %>
    <%= form.text_field :urlgithu, id: :proyecto_urlgithu %>
  </div>

  <div class="field">
    <%= form.label :reponsable %>
    <%= form.text_field :reponsable, id: :proyecto_reponsable %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
