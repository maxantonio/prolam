<%= form_with(model: proyecto, local: true) do |form| %>
  <% if proyecto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(proyecto.errors.count, "error") %> prohibited this proyecto from being saved:</h2>

      <ul>
      <% proyecto.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre, id: :proyecto_nombre ,:class=>"form-control"%>
  </div>

  <div class="field">
    <%= form.label :urlgithub %>
    <%= form.text_field :urlgithub, id: :proyecto_urlgithu,:class=>"form-control" %>
  </div>

  <div class="field">
    <%= form.label :user %>
    <%= form.collection_select :user_id, User.all, :id, :username,{prompt:true,autofocus: true ,class:"form-control"},class:"form-control"%>

  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
